'use strict';

function convertToRoman(num) {
  result = "";
  let numStr = num.toString();

  for (let i = 0; i < numStr.length; i++) {
    console.log(i);
  }

  return result;
}

convertToRoman(36);
